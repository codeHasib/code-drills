Challenge 2 — TypeScript
User Permission System

Folder:

typescript/user-permissions/

Create:

type Role = "admin" | "moderator" | "user";

Create a User type.

Build functions:

canDeleteProject()
canCreateWorkspace()
canInviteMembers()

Each function should return boolean.

Goal:

Union types
Business logic modeling
Function typing
